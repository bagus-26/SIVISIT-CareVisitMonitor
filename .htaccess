<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all requests to the public directory
    # This allows Laravel to work without /public in the URL
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L,QSA]

    RewriteRule ^ index.php [L]
</IfModule>
